# Optimizer Configuration (AdamW example)
# Contains: optimizer target, hyperparams (lr, weight_decay, betas, etc.)

_target_: torch.optim.AdamW

# Optimizer Hyperparams
lr: 3e-4
weight_decay: 1e-2
betas: [0.9, 0.999]
eps: 1e-8
