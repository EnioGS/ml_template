# Experiment Overrides (Example)
# Contains: overrides relative to defaults (swap groups + override values)

defaults:
  - override /model: resnet
  - override /trainer: default
  - override /datamodule: cifar10
  - _self_

# Overrides
trainer:
  max_epochs: 200

optimizer:
  lr: 1e-3

datamodule:
  batch_size: 256
